{% extends "AmicaleBundle::layoutUser.html.twig" %}

{% block titleX %}
    Abonnement
{% endblock titleX %}

{% block body %}


    <div class="row">
    <div class="col-lg-12">
        <div class="panel panel-default">
            <header class="style1">
                <h2>Liste des abonnements </h2>
            </header>
            <!-- /.panel-heading -->
            <div class="feature-list">
                {% for o in Abonnements %}
                {% if (loop.index / 2) %}
                <div class="row">
                    <div class="6u 12u(mobile)">
                        <section style="position: absolute; ">
                            <h3 class="icon fa-check">{{ o.titre }}</h3>
                            <p>Tunis,{{ o.lieux }},{{ o.Agence }},{{ o.type }},{{ o.abnDescription }}</p>
                            <p><strong>Date de début :</strong> <br>{{ o.DateDeb |date('d-m-Y')}} </p>
                            <p><strong>Date de fin :</strong><br>{{ o.DateFin |date('d-m-Y')}}</p>
                            <p><strong>Prix :</strong> {{ o.Prix }} $ </p>
                        </section>
                    </div>
                    {% endif %}
                    {% endfor %}
                </div>
                <!-- /.panel-body -->
            </div>
            <br>
            <br>
            <br>
            <br>
            <br>
            <br>
            <br>
            <br>
            <br>
            <br>
            <!-- /.panel -->
        </div>
        <!-- /.col-lg-12 -->
    </div>
    <!-- /.row -->
    {% spaceless %}
        <div> {{ socialLinks() }}</div>
    {% endspaceless %}

{% endblock body %}